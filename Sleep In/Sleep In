# Define a function to determine whether or I can sleep in

def sleep_in (weekday, vacation):

  # If it's not a weekday or if I am on vacation, return True  
  if not weekday or vacation:
      return True

  # Else, return False  
  else:
      return False
