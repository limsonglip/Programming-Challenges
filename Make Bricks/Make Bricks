# Define a function that returns True if we have sufficient small bricks (1 inch) and big bricks (5 inches) to reach the goal exactly
def make_bricks(small, big, goal):

  # Return True if we have enough big bricks to cover the entire goal and we have enough small bricks to cover the remaining length for distances not divisible by 5
  if big >= (goal // 5) and small >= (goal % 5):
      return True

  # Return True if we do not have enough big bricks to cover the entire goal but we do have enough small bricks to cover the remaining length
  elif big < (goal // 5) and ((small >= (goal - big*5)) or (small >= goal)):
      return True

  # Return False in all other scenarios
  else:
      return False
